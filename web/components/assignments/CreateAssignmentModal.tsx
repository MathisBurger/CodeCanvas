import { Group } from "@/service/types/tasky"


interface CreateAssignmentModalProps {
    group: Group|null
}

const CreateAssignmentModal = ({group}: CreateAssignmentModalProps) => {


    return (
        <Modal
    )
}
