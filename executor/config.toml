[coordinator.api]
endpoints.health = true
endpoints.jobs = true
endpoints.tasks = true
endpoints.nodes = true
endpoints.queues = true
endpoints.stats = true

[runtime]
type = "docker"

[datastore]
type = "postgres"

[datastore.postgres]
dsn = "host=localhost user=executor password=executor dbname=executor port=5432 sslmode=disable"
